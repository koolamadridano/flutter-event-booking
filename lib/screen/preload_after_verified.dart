import 'package:flutter/material.dart';

class PreloadAfterVerified extends StatefulWidget {
  const PreloadAfterVerified({Key? key}) : super(key: key);

  @override
  State<PreloadAfterVerified> createState() => _PreloadAfterVerifiedState();
}

class _PreloadAfterVerifiedState extends State<PreloadAfterVerified> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.white,
      body: Container(),
    );
  }
}
